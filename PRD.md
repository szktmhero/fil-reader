大容量のログファイル（TSVやパイプ区切りなど）を柔軟に扱い、かつ高速に検索できるようにするため、区切り文字の動的設定機能と検索機能をコア要件に組み込んだ、アップデート版のプロダクト要求仕様書（PRD）を提示します。

---

# プロダクト要求仕様書 (PRD) - 大容量ログ・CSVビューア (ベータ版)

## 1. プロダクト概要

本プロダクトは、10GBクラス（数千万～数億行）の超大容量なログファイルやCSV・TSVファイルを、メモリ不足（OOM）やUIのフリーズを起こすことなく高速に閲覧・検索できるWindowsデスクトップアプリケーション（WPF）です。
設定ファイルによって任意の区切り文字（タブ、パイプなど）に対応し、インプロセスデータベース（SQLite）の強力なインデックス機能を活用して、巨大なデータからの高速なキーワード検索を実現します。

## 2. 目的と背景

* **課題:** 開発やシステム運用で出力される巨大なログファイル（.log, .tsv, .datなど）は、標準のテキストエディタでは開くことすらできず、特定の特定のエラーコードやキーワードを抽出して閲覧することが困難である。
* **目的:** 区切り文字の異なる様々なシステムログを柔軟に取り込み、快適に閲覧・検索できる軽量かつ強力なビューアを提供する。

## 3. スコープ（ベータ版）

### 含まれるもの (In-Scope)

* 10GBクラスの各種テキストログファイルの読み込みと表示。
* **設定ファイルによる区切り文字（Delimiter）の動的切り替え。**
* 複数ファイルの同時展開（タブベースのUI）。
* **指定した列、または全列を対象とした高速キーワード検索（フィルタリング）。**
* スムーズな無限スクロール（データ仮想化を用いたオンデマンド読み込み）。

### 含まれないもの (Out-of-Scope)

* データの編集および変更後のファイル保存機能。
* 複数ファイルを跨いだ複雑な結合（JOIN）検索。
* 正規表現による高度なパターンマッチング（ベータ版では部分一致・前方一致などの単純検索に限定）。

---

## 4. 機能要件 (Functional Requirements)

| ID | 機能名 | 詳細 |
| --- | --- | --- |
| **F-01** | **設定ファイルによるフォーマット定義** | アプリ配置フォルダ内の設定ファイル（`config.json`等）に、対象ファイルの拡張子、区切り文字（ `,` / `\t` / `|` ）、ヘッダーの有無を定義できる。 |
| **F-02** | **可変区切り文字でのインポート** | 選択されたファイルの拡張子やユーザー指定に基づき、`CsvHelper`の設定を動的に変更してパースし、一時DB（SQLite）へ高速にバルクインサートする。 |
| **F-03** | **バックグラウンドインポート＆進捗表示** | 読み込み処理は非同期で実行し、UIをブロックしない。インポート中の進捗（読込行数/秒など）を画面上にリアルタイム表示する。 |
| **F-04** | **タブ型複数ファイル表示** | フォーマットの異なる複数のログファイルを、それぞれ独立したタブとして同時に開いて並行閲覧できる。 |
| **F-05** | **仮想化データグリッド** | UI仮想化を有効にした `DataGrid` を用い、画面に表示されている行のデータのみをSQLiteから都度取得して描画する。 |
| **F-06** | **高速キーワード検索・フィルタ** | 画面上に検索バーを配置。キーワード入力時、SQLの `LIKE` 句やインデックスを活用し、数千万行から該当行のみを瞬時に絞り込んで表示する。 |
| **F-07** | **自動クリーンアップ** | タブ閉鎖時、またはアプリ終了時に、生成された一時DBファイルを物理削除する。起動時には過去の残存キャッシュを自動検知して一掃する。 |

---

## 5. 非機能要件 (Non-Functional Requirements)

| カテゴリ | 詳細 |
| --- | --- |
| **パフォーマンス** | ・インポート中もUIは完全に操作可能であること。<br>

<br>・検索実行時、インデックスが効くクエリであれば10GBのデータに対しても数秒以内に結果を返すこと。 |
| **リソース管理** | 検索やスクロールを繰り返しても、メモリ使用量が一定水準（500MB以下）を超えて増大しないこと（メモリリークの防止）。 |
| **拡張性** | 新たなログフォーマット（スペース区切りなど）が追加された場合でも、ソースコードを修正することなく設定ファイルの記述追加だけで対応できること。 |
| **堅牢性** | ログ内に破損した行（列数が足りない、バイナリ混入など）があっても、その行をスキップするか空白埋めにして処理を続行し、全体クラッシュを防ぐこと。 |

---

## 6. 技術スタック・実装のポイント

* **設定の管理:** `Microsoft.Extensions.Configuration`（C#標準のJSON設定機能）を使用。
* **高速検索の実現:**
* SQLiteへのインポート完了後、検索対象となる主要な列に対して `CREATE INDEX` を実行する。これにより、10GB（数千万行）あっても `SELECT * FROM table WHERE col LIKE 'key%'` といった前方一致検索や完全一致検索がミリ秒単位で完了する。
* 全列を対象にした曖昧検索が必要な場合は、SQLiteの **FTS5（Full-Text Search）** 機能の拡張モジュールを有効化し、全文検索インデックスを構築する設計を検討する。


* **CSV/ログパーサー:** `CsvHelper` の `CsvConfiguration` オブジェクトに対して、設定ファイルから読み込んだ `Delimiter` 文字（`\t` や `|` など）を動的に代入してストリーム読み込みを行う。

---

## 7. リスクと対策

1. **検索インデックスによる容量増大:** インデックスを貼ることで検索は劇的に高速化しますが、一時DB（.db）のファイルサイズが元ファイル（10GB）の1.5倍～2倍程度に膨らむ可能性があります。ドライブの空き容量チェックをインポート前に厳しめに行う仕様とします。
2. **初回インポート時間:** 10GBをDB化してインデックスを構築するまでの時間はどうしても発生します。まずは「インデックスなし」で最速で1画面目を表示し、検索機能は裏でインデックス構築が完了した後に有効化する、といった段階的ロード（UXの工夫）が有効です。